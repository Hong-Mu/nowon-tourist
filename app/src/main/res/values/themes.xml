<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.NowonTourist" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="buttonStyle">@style/Theme.NowonTourist.Button</item>
        <item name="editTextStyle">@style/Theme.NowonTourist.EditText</item>
        <item name="android:statusBarColor">@color/green</item>
        <item name="bottomSheetDialogTheme">@style/Theme.NowonTourist.BottomSheetDialog</item>
    </style>

    <style name="Theme.NowonTourist" parent="Base.Theme.NowonTourist" />

    <style name="Theme.Full" parent="Base.Theme.NowonTourist">
        <item name="android:windowFullscreen">true</item>
    </style>
</resources>